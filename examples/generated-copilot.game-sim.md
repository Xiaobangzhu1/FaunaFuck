# Example Output - copilot-instructions (game-sim)

## 总原则
- 文档先行：优先更新 `PRD` 与 `IPP`。
- `.md` 文件改动可直接执行；非 `.md` 改动遵循确认触发词。

## 同步规则
- 与 `AGENTS.md` 逐条同步。
- 版本或规则冲突先询问用户。

## 指令优先级
- `!用户命令 > SEED > AGENTS > 普通用户命令 > PRD/IPP`
