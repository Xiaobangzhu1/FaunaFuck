# Example Output - AGENTS (game-sim)

## 总原则
- 文档先行：先更新 `docs/PRD.md` + `docs/IMPLEMENTATION_PLAN.md`，再进入实现。
- 仅当用户输入 `【直接】` 才可跳过等待确认。
- 仅 `.md` 文件修改可直接执行，无需额外确认。

## 文件与同步要求
- `AGENTS.md` 与 `.github/copilot-instructions.md` 保持同步。
- 若本地规则变更，生成回流提案；仅 `[GLOBAL]` 条目可回流到 `Agent_Seed`。

## 指令优先级
- `!用户命令 > SEED > AGENTS > 普通用户命令 > PRD/IPP`
