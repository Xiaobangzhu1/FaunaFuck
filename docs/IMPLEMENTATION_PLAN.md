# IMPLEMENTATION_PLAN - 文档拆分与实施计划

## 1. 与 PRD 对齐说明

- 本计划对应 `docs/PRD.md` 的高层目标，不在此重复系统规则细节。
- 系统规则统一引用系统设计文档。
- 本轮目标是完成文档拆分、主权收敛与冲突登记。

## 2. 任务清单

### 2.1 文档拆分落盘

- [x] 新增仓库根目录 `README.md`，说明项目定位、运行方式、测试方式与文档入口
- [x] 将 `docs/PRD.md` 收敛为高层需求文档
- [x] 新增系统设计文档（核心循环、实体状态、细胞行为、可视化调试、规则边界）
- [x] 保留并引用 `docs/DNA_RULES.md` 作为转录规则细节文档
- [x] 新增 `docs/README.md` 作为文档索引与边界说明
- [x] 新增 `docs/archives/prd-history.md` 迁入历史阶段内容

### 2.2 风险收敛与冲突治理

- [x] 在系统设计文档中统一登记当前冲突与不确定项为 `待确认`
- [x] 采用“系统文档为规则细节主权、PRD 为高层主权、IPP 为任务主权”
- [x] 确保同一条规则只在一个主文档完整定义

### 2.3 协作规则同步

- [x] 更新 `AGENTS.md`：明确“系统设计文档与 PRD 同级前置”
- [x] 同步更新 `.github/copilot-instructions.md` 保持一致
- [x] 若目录结构变化，同步更新 `STRUCTURE.md`

### 2.4 历史需求与后续待办

- [ ] 运行中实时调出日志能力（规则已入文档，待实现）
- [ ] 启动默认输入 `spawn`（规则已入文档，待实现）
- [x] 新细胞生成默认优先按父细胞朝向方向尝试
- [x] 修改存档模式并引入运行时存/读档指令
- [x] 复制指令引入概率失败机制（失败时跳过该指令）
- [x] 控制台支持历史命令选中复制，且 `Left/Right` 在输入非空时仅移动插入点
- [ ] 统一方向显示口径在 `PRD` / 系统文档 / `FRONTEND_GUIDELINES` 的冲突

### 2.5 SEED 文档与独立仓库交付

- [x] 生成 `SEED.md`（汇总 AGENTS 规则与项目方向）
- [x] 在 `SEED.md` 写入“新智能体拿到后自动生成 AGENT”的流程与未知项提问协议
- [x] 在 `SEED.md` 写入独立 GitHub 仓库地址占位与命名规范
- [x] 输出独立仓库命名方案（至少 3 个候选）供用户选择（已确定仓库名：`Agent_Seed`）
- [x] 提供至少 3 种独立仓库上传方案（如单文件仓库 / 模板仓库 / Gist+镜像仓库）
- [x] 选定一种方案并实际上传 `SEED.md` 到独立仓库（目标：`Agent_Seed`）
- [x] 在 `AGENTS.md` / `.github/copilot-instructions.md` 增加“SEED 与 AGENTS 同步 + 冲突先询问用户”规则
- [x] 在 `SEED.md` 增加“多项目类型模板”结构（至少覆盖 3 类项目）
- [x] 在 `SEED.md` 增加“先询问项目类型再生成 AGENT”的标准问答入口
- [x] 更新规则：仅 `.md` 文件改动可直接执行，无需额外确认
- [x] 固化优先级：`!用户命令 > SEED > AGENTS > 普通用户命令 > PRD/IPP`
- [x] 新增模板库目录：`templates/`（4 类项目模板）
- [x] 新增示例目录：`examples/`（AGENTS 与 copilot 生成示例）
- [x] 新增 `CHANGELOG.md`（SEED 版本变更记录）
- [x] 固化回流机制：每次本地 AGENTS 变更即提案，仅 `[GLOBAL]` 条目可回流

### 2.6 架构示例文档交付

- [x] 新增具体架构示例文档（治理层/项目层/产品层三层结构）
- [x] 在示例文档中明确输入输出、冲突处理、同步机制与验收清单
- [x] 将示例文档加入 `docs/README.md` 与 `STRUCTURE.md` 索引

## 3. 合理顺序

- [x] 先收敛 PRD 边界
- [x] 再拆系统设计文档并建立索引
- [x] 最后同步 AGENTS / copilot / STRUCTURE / progress

## 4. 完成定义

- `PRD` 可在不读细节文档时说明项目目标与成功标准。
- 系统文档可单独解释对应子系统如何运作。
- `IPP` 不再承担规则解释职责。
- 所有不确定内容已显式标记 `待确认`。
